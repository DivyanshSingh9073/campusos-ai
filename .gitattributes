# Enforce LF line endings for all text files to ensure cross-platform consistency.
# Git will handle conversions automatically based on this setting.
* text=auto eol=lf

# Explicitly mark common binary file types to prevent Git from changing their line endings.
*.{png,jpg,jpeg,gif,ico} binary
*.{eot,ttf,woff,woff2} binary
*.pdf binary
